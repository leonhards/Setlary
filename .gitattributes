# Set the default behavior, in case core.autocrlf isn't set.
* text=autocrlf

# Declare text files that will always be normalized and converted
# to native line endings on checkout.
.editorconfig   text
.gitattributes  text
.gitignore      text
*.css           text
*.scss          text
*.js            text
*.json          text
*.map           text
*.md            text
*.txt           text
*.xml           text

# Declare files that will always have LF line endings on checkout.
*.php text eol=lf

# Denote all files that are truly binary and should not be modified.
*.jpg binary
*.jpeg binary
*.ttf binary
*.woff2 binary